% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aquatic_AR-p_fit_functions.R
\name{fit_seasonal_ARp_models}
\alias{fit_seasonal_ARp_models}
\title{Fit seasonal AR-p_beta model}
\usage{
fit_seasonal_ARp_models(
  model_pars,
  fit_gauss = TRUE,
  fit_flat = TRUE,
  iter = 2000,
  warmup = 1000,
  mtd = 18,
  ad = 0.99
)
}
\arguments{
\item{model_pars}{a list of parameters describing the AR-p time series as well
as a matrix of covariates, vectors of beta and phi parameters, and a simulated
time series. Must include:
\itemize{
\item n: length of time series
\item p: number of AR lags to consider
\item tau_0: the prior guess for tau0
\item X: matrix of covariates arranged with the first column as the intercept,
columns 2:(nlags covariate + 1) are lagged versions of covariate 1, and the
remaining columns are other covariates.
\item y: response timeseries
\item holdout: number of observation to hold out for model evaluation
}}

\item{fit_nr}{(default = TRUE) - should a non-regularized model also be fit to the data?}
}
\value{
A list including model input parameters and model fit objects for any models run
}
\description{
This function fits an AR-p_beta model on a simulated dataset with the option
to fit both a regularized and non regularized model version
}
