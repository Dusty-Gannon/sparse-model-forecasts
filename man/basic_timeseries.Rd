% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim1_temporal_drivers.R
\name{basic_timeseries}
\alias{basic_timeseries}
\title{Simulate a time series using temporally autocorrelated covariates}
\usage{
basic_timeseries(
  K,
  num_strong,
  n,
  freq,
  trend_fraction = 0.5,
  prob_cycle = 0.5,
  sigma = 0.5
)
}
\arguments{
\item{K}{Number of drivers.}

\item{num_strong}{Number of strong drivers}

\item{n}{Length of time series}

\item{freq}{Frequency of sampling per time step (e.g., \code{n = 5} and
\code{freq = 365}) would create 5 years of daily data.}

\item{trend_fraction}{Fraction of drivers that have a trend.}

\item{prob_cycle}{Probability that a driver experiences a seasonal cycle.}

\item{sigma}{Standard deviation of the noise component.}
}
\value{
List with the response, \code{y}, the model matrix, \code{X},
and the regression coefficients used to construct the response, \code{beta}.
}
\description{
This function simulates a time series by creating autoregressive and
seasonal drivers and constructing a response based on a linear combination
of those drivers.
}
\examples{
# simulating a 3-year time series with data from each day,
# constructed with 10 drivers, 3 strong and 7 weak.

basic_timeseries(
  K = 10,
  num_strong = 3,
  n = 3,
  freq = 365
)


}
