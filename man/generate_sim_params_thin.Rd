% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/terrestrial_ricker_sim_functions.R
\name{generate_sim_params_thin}
\alias{generate_sim_params_thin}
\title{Generate simulation parameters for single TS thinning experiments}
\usage{
generate_sim_params_thin(
  x,
  nsp = 40,
  init_steps = 100,
  tot_steps = 500,
  num_ngs = 3,
  sigma_rng = c(0.1, 0.5),
  alpha_rng = c(0.005, 0.01),
  lambda_rng = c(1.2, 1.8),
  ng_range = c(0.2, 0.4),
  rho = 0,
  mean_init_abund = 20,
  comp_matrix_type = 2,
  thin_freq = 2,
  thin_factor = 0.1,
  target_thin = TRUE
)
}
\arguments{
\item{x}{Dummy variable so that this function is straight forward to use with apply statements}

\item{nsp}{Number of species to start with}

\item{init_steps}{Number of steps before thinning}

\item{tot_steps}{Total number of steps over which to simulate}

\item{num_ngs}{Number of non-generic species}

\item{sigma_rng}{Interval over which to draw scales of demographic stochasticity}

\item{alpha_rng}{Interval from which to draw intraspecific competition coefficients}

\item{lambda_rng}{Interval from which to draw low-density growth rates}

\item{ng_range}{Range over which to draw the fraction of intra-specific growth for the non-generics
(i.e., 0.5 means that non-generic competition coefficient would be half as large as intra-specific
competition)}

\item{rho}{Fraction of intraspecific competition that is the generic competitive effect}

\item{mean_init_abund}{Average initial abundance for the random initial states}

\item{comp_matrix_type}{Non-generic competition can either be random (\code{comp_matrix_type = 1})
or structured so that no species gets overloaded with strong competitors (\code{comp_matrix_type = 2}).}

\item{thin_freq}{Frequency of thinning treatments. \code{thin_freq = 0} will proceed without
thinning treatments, while a value of \code{thin_freq = c} with c > 0, will perform a
thinning treatment every c years.}

\item{thin_factor}{Factor by which to thin a species. \code{thin_freq = 0.1} with thin a
species to 10 percent of its population density in the previous year.}
}
\value{
List of simulation parameters
}
\description{
Generate simulation parameters for single TS thinning experiments
}
