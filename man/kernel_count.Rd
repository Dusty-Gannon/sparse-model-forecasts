% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/terrestrial_sim_functions.R
\name{kernel_count}
\alias{kernel_count}
\title{Count neighbors of each type in a neighborhood}
\usage{
kernel_count(X, r, sp_list)
}
\arguments{
\item{X}{Matrix with species IDs in each cell in the lattice}

\item{r}{Radius of the neighborhood (in a lattice)}

\item{sp_list}{List of species IDs. Should be numeric and starting at 1 (e.g, 1--4 for a 4 species model)}
}
\value{
An array with \eqn{S} slices of matrices of equal dimension to X, where \eqn{S} is the number
of species in the model. Each slice represents the count of species \eqn{s=1,2,...,S} in the neighborhood
around each cell in X.
}
\description{
Count neighbors of each type in a neighborhood
}
\examples{
X <- matrix(sample(1:3, 16, replace = T), nrow = 4, ncol = 4)
kernel_count(X, r = 1, sp_list = 1:3)

}
