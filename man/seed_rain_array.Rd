% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/terrestrial_sim_functions.R
\name{seed_rain_array}
\alias{seed_rain_array}
\title{Seed rain array}
\usage{
seed_rain_array(F_mat, X, d_max, rate = 1, nsp)
}
\arguments{
\item{F_mat}{Matrix of fecundities for each individual in the lattice}

\item{X}{Lattice with numeric species IDs (1, 2, 3, ..., S) for the individual in each cell}

\item{d_max}{Max distance (vertical and horizontal, not diagonal) for dispersal (currently
set as a global parameter and is not species-specific).}

\item{rate}{Exponential rate parameter(s) for each species defining the dispersal kernel. This
can be a vector of length \eqn{S}, where \eqn{S} is the number of species in the model, or a scalar,
giving all species equal dispersal distributions.}
}
\value{
An array with \eqn{S} slices made up of \eqn{M \times J} matrices, where \eqn{M \times J}
is the dimension of the lattice.
}
\description{
Construct an \eqn{M \times J \times S} array encoding the total seed rain into each location in the \eqn{M \times J}
lattice for each of \eqn{S} species.
}
\examples{
# lattice
X <- matrix(sample(1:3, 16, replace = T), nrow = 4)

# fecundity for each individual
F_mat <- matrix(rgamma(16, shape = 10), nrow = 4)

# max dispersal distance
d_max <- 2

# exponential dispersal rates
rate <- c(0.5, 1, 2)

# compute seed rain array
seed_rain_array(F_mat, X, d_max, rate)


}
