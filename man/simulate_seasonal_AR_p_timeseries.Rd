% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aquatic_AR-p_sim_functions.R
\name{simulate_seasonal_AR_p_timeseries}
\alias{simulate_seasonal_AR_p_timeseries}
\title{Simulate seasonal AR-p timeseries}
\usage{
simulate_seasonal_AR_p_timeseries(input_pars = NULL)
}
\arguments{
\item{input_pars}{a list of parameters describing the AR-p model to be generated.
Default values for all parameters are embedded in the function, so only
supply parameters you wish to change.
\itemize{
\item n (300): length of time series to be simulated
\item p (16): number of AR lags to consider
\item beta_p (5): number of lags to consider in lagged covariate
\item beta_n (45): number of additional covariates to include
\item b0 (0.5): intercept
\item n_phi (3): number of non-zero autoregressive terms
\item n_lags (2): number of non-zero lags in lagged covariate
\item n_beta (2): number of non-zero covariate parameters in addition to lagged covariate
\item non_zero_coef_guess (0): guess for the number of non-zero coefficients
\item holdout (50): number of observation to hold out for model evaluation
}}

\item{draw_beta}{can be 'near_zero' or 'zero'. Indicates if the not-significant
beta parameters should be drawn from a normal distribution that results in numbers
that are near zero, or if they should just be set to zero}

\item{draw_phi}{can be 'near_zero' or 'zero'. Indicates if the not-significant
phi parameters should be drawn from a normal distribution that results in numbers
that are near zero, or if they should just be set to zero.}

\item{burnin}{number of steps to run the AR timeseries simulation before using numbers}
}
\value{
A list including input parameters or any default values used as well
as a matrix of covariates, vectors of beta and phi parameters, and a simulated
time series.
}
\description{
This function simulates an AR process from seasonal components and
generates a model matrix with sparse Fourier components
}
\examples{

input_pars <- list(
  n = 365,           # length of time series
  phi = c(0.5, 0.1), # vector of AR terms
  sd = 1,            # standard deviation of the innovations
  beta_n = 5,        # number of covariates to include
  beta_p = 0,        # number of lags of covariate 1 to include
  K = 100,           # number of fourier components to include
  holdout = 50
)

simulate_seasonal_AR_p_timeseries(input_pars)

}
